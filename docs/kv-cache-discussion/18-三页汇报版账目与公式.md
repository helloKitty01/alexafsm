# 18 · 三页汇报版（v3.6.1）账目与公式底稿

> `kv cache方案（汇报版）.v3.6.1.slides.html` P2 / P3 的数据底稿。全部数字由 [16](./16-数据基线-优化后.md) / [17](./17-数据基线-优化前.md) 的基线算出，页内 `BASELINE` 对象即同一份数；改基线一处、全稿同步。
> 单位 K token。口径：两边同内容只换排布；会话内缓存不被逐出；无裁剪；不计块粒度误差。
> 基线：Tools 5.9 / System Prompt 固定 28.9 / 会话级 1.5（user.md 0.5 + device_info 0.5 + memory.md 0.5）/ 历史 4 / UAT-U 动态 3.5（available skills 3 + 动态记忆 0.3 + 前台应用 / 小艺状态 / 位置 0.05）/ query 0.1 / tool call 0.2 / 检索 10 / skill 5 / 工具 2.5 / answer 0.25。
> **v3.6 相对 v3.5 的改动**：① 调用链按实际改——检索结果与 preload 的 skill 以**伪造 A（tool call）/ T（result）** 随 U 进入首调输入，不占模型调用；问答 1 次调用、任务 2 次、闲聊 1 次。② 修 P2 当前侧矩阵 bug：v3.5 及之前前缀第 2 / 3 列（动态 / 会话级）格色写反，Q2–Q4 首调的动态格现在正确显示为红色（原位重写）。③ 分桶表"占总节省"列换成"cache 命中率 当前 → 目标"。④ 三句话加第四条"效果不变"。⑤ 页脚加实测对照占位（当前约 60% / 12K，目标 90% / 7K）。**v3.6.1**：概念解释里注明实算的内部口径叫"硬算"；P2 矩阵每行账目末尾加该次调用的 cache 命中率 chip，行标下写演示会话 5 次加权 58% / 88%。

## 0. 调用链与两个必算口径

| 业务 | 占比 | 消息流 | 模型调用 | 首调必算（n≥2） | 第二次调用 |
| --- | ---: | --- | ---: | ---: | ---: |
| 问答 | 47% | Δ+U → 伪造 A 0.2 / T 检索 10 → **调①** answer | 1 | 3.85 + 10.2 = **14.05** | — |
| 任务 | 25% | Δ+U → 伪造 A 0.2 / T skill 5 → **调①** tool call 0.2 → T 工具 2.5 → **调②** answer | 2 | 3.85 + 5.2 = **9.05** | 2.7 |
| 闲聊 | 28% | Δ+U → **调①** answer | 1 | **3.85** | — |

- **首调必算** = 动态 3.5 + query 0.1 + 上轮 answer 0.25 = **3.85K**（闲聊基准，页面显示 3.9K）+ 当轮业务必带（伪造 A/T：问答 10.2 / 任务 5.2）。决定 **TTFT**。业务必带两边相同、预热不覆盖。
- **整轮实算 T(n≥2)** = 业务加权（14.05 × .47 + 9.05 × .25 + 3.85 × .28）+ 任务第二次调用 2.7 × .25 = **10.6K**。决定**成本**。
- 沉淀速率 α（当前方案下每 Q 进 UAT、下一 Q 被整段白算）= .47 × 10.55 + .28 × 0.35 + .25 × 8.25 = **7.1K/轮**。当前方案动态在 System Prompt 内不沉淀；目标方案动态留在 U 里，输入每 Q 多 3.5K，已计入命中率分母。

## 1. 账目逻辑与目标（P2 底部两行）

- ① 输入 = 已缓存 + 白算 + 必算。② **实算**（内部口径叫"**硬算**"，即引擎逐请求上报的非缓存 prefill token）= 白算 + 必算 = 输入 − 已缓存，账单与 TTFT 跟它走。③ **cache 命中率** = 已缓存 ÷ 输入（业界口径，分母含必算）。

| 目标 / 尺子 | 演示会话（5 次调用） | 说明 |
| --- | ---: | --- |
| 目标 ① 白算 → 0 | 88.6K → 0 | 验收线；目标态 loop 白算 > 1 块即报警 |
| 目标 ② 必算尽可能小 | 38.8K = 38.8K | 整轮必要新增，两边相同；下一步靠 available skills 升会话级、检索 / 工具结果精简 |
| 成本看实算降幅 | 127.3K → 38.8K（−70%） | 省额 = 白算；Q≥2 首调 31.0–34.4K → 3.9–9.1K |
| cache 命中率 | 全业务加权 55% → 86% | 随轮数上升：6 轮 92%、12 轮 95%；1Q 75%，一半会话只有 1 轮 |

## 2. P2 演示会话（Q1 问答 · Q2 任务 · Q3 闲聊 · Q4 闲聊，共 5 次调用）

| 调用 | 当前 输入 | 当前 实算 | 其中白算 | 当前 命中 | 目标 输入 | 目标 实算 | 目标 命中 | 省 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Q1 调① 会话首调（问答，含伪造 A/T 检索 10.2） | 54.1 | 25.2 | 5.9（Tools） | 53% | 54.1 | 19.3 | 64% | 5.9 |
| Q2 调① Q 首调（任务，含伪造 A/T skill 5.2） | 59.6 | 31.0 | 21.9（固定项 11.4 + 本会话史 10.55） | 48% | 63.1 | 9.1 | 86% | 21.9 |
| Q2 调② 工具调用（tool call 0.2 + 工具 2.5） | 62.4 | 2.7 | 0 | 96% | 65.8 | 2.7 | 96% | 0 |
| Q3 调① Q 首调（闲聊） | 62.7 | 34.0 | 30.2（11.4 + 18.8） | 46% | 69.7 | 3.9 | 94% | 30.2 |
| Q4 调① Q 首调（闲聊） | 63.0 | 34.4 | 30.5（11.4 + 19.15） | 46% | 73.5 | 3.9 | 95% | 30.5 |
| **合计** | 302 | **127.3** | **88.6** | **58%** | 326 | **38.8** | **88%** | **88.6（−70%）** |

固定项 11.4 = Tools 5.9 + 会话级 1.5 + 历史 4。省额 = 白算；必算两边都是 38.8（含会话首调载入 5.5）；演示会话 5 次加权命中率 58% → 88%（P2 矩阵行标与逐行 chip 显示；底部尺子只显示全业务加权 55% → 86%）。

矩阵 UAT 10 列：U1 · A₁T₁(检索) · ans₁ · U2 · A₂T₂(skill) · A₂′T₂′(工具) · ans₂ · U3 · ans₃ · U4。当前侧前缀列序：固定 · 动态 · 会话级 · Tools · 历史。

## 3. P3 轮数公式

- ΔS(1) = 5.9（Tools）；**ΔS(n≥2) = 11.4 + 7.1×(n−1)**
- C(1) = 21.8；**C(n≥2) = 22.0 + 7.1×(n−1)**；T(1) = 15.9；**T(n≥2) = 10.6** 恒定；η = ΔS / C

| n | C(n) | T(n) | ΔS(n) | η(n) | cache 命中率 当前 → 目标 |
| ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 21.8 | 15.9 | 5.9 | −27% | 65% → 75% |
| 2 | 29.1 | 10.6 | 18.5 | −64% | 60% → 86% |
| 3 | 36.3 | 10.6 | 25.6 | −71% | 55% → 88% |
| 4 | 43.4 | 10.6 | 32.8 | −76% | 52% → 90% |
| 5 | 50.5 | 10.6 | 39.9 | −79% | 49% → 91% |
| 6 | 57.6 | 10.6 | 47.0 | −82% | 47% → 92% |
| 8 | 71.9 | 10.6 | 61.2 | −85%（外推） | 43% → 93% |
| 12 | 100.3 | 10.6 | 89.7 | −89%（外推，≥11 轮桶按此计） | 38% → 95% |

曲线画 Q1–Q6，命中率纵轴 40–100%。全业务加权命中率 **55% → 86%**。相对 v3.5（67% → 91%）下降的原因：问答不再有第二次高命中率的 loop 调用，检索 10K 进了首调分母——口径更接近实际（线上实测当前约 60%）。

### 3b. 首调 TTFT 预估

TTFT ≈ 首调实算 × 46 ms/K（参考实测 14K prefill ≈ 640 ms）。表按闲聊基准；预热后统一 170 ms（动态 + query ≈3.6K ≈165 ms + 预热损耗）。

| 轮次 | 当前首调实算 → TTFT | 目标 | 目标 + 预热 |
| --- | ---: | ---: | ---: |
| Q1（会话首调） | 15.0K · 690 ms | 9.1K · 420 ms | 170 ms |
| Q2 | 22.4K · 1.0 s | 3.9K · 180 ms | 170 ms |
| Q6 | 50.8K · 2.3 s | 3.9K · 180 ms | 170 ms |
| Q12 | 93.6K · 4.3 s | 3.9K · 180 ms | 170 ms |

**当轮业务必带、两边相同、预热不覆盖**：问答 +伪造 A/T 检索 10.2K ≈ +470 ms，任务 +skill 5.2K ≈ +240 ms。

**预热**：会话创建时 prefill 会话级 + 历史（5.5K）；每轮开口前（ASR 阶段）再灌上轮 answer。收益 / 代价（参考实测）：14K 一次 640 ms → 6K 预热 320 + 8K 实时 380 ms，感知 −40%；总算力 +10%；需 TTL 保住预热到开口之间的缓存。只改 TTFT，不改账。

## 4. 按会话长度分桶（完整轮数分布，≥11 轮按 12 轮计）

原始分布合计 99.17%，差额按比例并入 1–5Q（页面不写）。≥11 轮按 12 轮计：当前侧首调输入到第 12 轮 ≈122K，128K 窗口填满。

| 会话长度 | 占比 | 当前 / 会话 | 目标 / 会话 | 降幅 | 占当前算力 | cache 命中率 当前 → 目标 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 1Q | 51.5% | 21.8 | 15.9 | −27% | 12% | 65% → 75% |
| 2Q | 20.2% | 50.9 | 26.5 | −48% | 11% | 62% → 81% |
| 3Q | 9.1% | 87.2 | 37.1 | −57% | 9% | 60% → 84% |
| 4Q | 5.6% | 130.5 | 47.7 | −63% | 8% | 57% → 86% |
| 5Q | 3.5% | 181.0 | 58.3 | −68% | 7% | 55% → 87% |
| 6–10Q | 6.3% | 均 338 | 均 84 | −75% | 23% | 51% → 89% |
| 11Q+（按 12） | 3.9% | 734 | 133 | −82% | 31% | 46% → 92% |
| **加权** | 100% | **92.8** | **32.0** | **−65%** | 100% | **55% → 86%** |

6Q+ 合计：**10% 的会话吃掉 54% 的算力**，贡献 65% 的节省（后者不进表，三句话①引用）。

## 5. 三句话 + 效果（P3）

1. **会话数 10% / 算力 54%**（迷你横条）：6Q+ 的长会话只占 10% 的会话数，却吃掉 54% 的算力，贡献 65% 的节省；越长省得越多，Q2 −64% → Q12 −89%。
2. **3.9K**：会话级可提前算，首调时延与轮数脱钩。Q≥2 首调钉在 3.9K + 当轮检索 / skill（当前 22–94K 随轮涨）；加预热后 TTFT ≈ 170 ms + 业务必带。
3. **白算 → 0**：验收线；整轮必算 10.6K 是剩下的全部成本，available skills 3K 若升为会话级、首调必算 → 0.9K。命中率受必算拖累，加权 86%、长会话 90%+。
4. **效果不变**：动态下沉 U、随轮追加已上线，对当前对话效果影响不大（定性，暂无指标）；后两步只改顺序不改内容，不引入新的效果风险。

## 6. 口径边界（页脚已写）

1. −65%（全业务）/ −70%（演示会话）是**实算 token** 降幅；费用按实际计价折算，TTFT 由实测验证。
2. cache 命中率全业务加权 55% → 86%；实测对照：当前线上约 60%、平均实算约 12K/调用，目标 90% / 7K，上线后以实测替换推演。**验收看白算归零，命中率按业界口径一并报。**
3. 前提是会话内缓存不被逐出。实测 21 次调用里 2 次 loop 全 miss 属引擎侧逐出 / 路由，需会话粘性 + TTL 配合并单独报警。
4. ≥11 轮按 12 轮计是下限口径，不外推。
5. TTFT 按 46 ms/K 线性折算、预热后统一 170 ms，待 E6 实测替换。
6. 未计入：available skills 升会话级、动态"只带变化"、检索 / 工具结果精简可再压必算，是下一步。上线后由日报（引擎 `cached_tokens` + 应用边界埋点）替换推演。
