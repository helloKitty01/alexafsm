# 14 上下文组成实测数据需求(填充 P1 虚框与 P3–P9 参数)

> 目的:把 [上下文组成对比.v1.1](./上下文组成对比.v1.1.slides.html) 上的虚框填成实测值,
> 并锁定 [13-汇报大纲](./13-汇报大纲.md) P3–P9 的全部参数。
> 本篇是 [12](./12-上下文长度实测需求.md) 的续篇:12 的模块划分基于旧的 L0/L1/L2 口径,
> 本篇按 v2 的**四级分级**重新定义模块;12 中已测得的值直接沿用(见 §4)。
> 不知道的先留空,能给"包含关系 + 合计"也行,在 note 里说明。

---

## 1. 统一口径(必须遵守)

1. **单位一律为 token**,用与推理侧相同的 tokenizer;
2. 对 **chat template 渲染后的最终序列**计数(含控制 token),不是原始文本 / JSON;
3. 分布字段给 **均值 / p50 / p90**;固定内容给单值;
4. 注明样本范围(时间窗、会话数、调用数);
5. 有裁剪 / 压缩触发的会话请在 note 标注占比,避免污染分布。

## 2. 分级与模块定义

物理序:当前 `SP → Tools → UAT`;目标 `Tools → SP → UAT`。
下表按目标态分级列模块,"当前对应"一列说明现网里它在哪、怎么识别。

| # | 级 | 模块 | 目标态位置 | 当前对应 / 怎么识别 | 需要的数据 |
| --- | --- | --- | --- | --- | --- |
| A1 | 产品级 | **Tools 块** | 最前 | 现网 tools 参数渲染后的段落。抽不同机型 / ROM 的会话对比:**工具清单是否全网一致**;若不一致,给变体数与各变体流量占比 | 工具清单(名称 + 单个 schema 渲染后长度)、总长、是否按 ROM / 已安装应用变化;目标态元工具集若已定义,同样给清单与总长 |
| A2 | 产品级 | **SP 固定** | Tools 之后 | system prompt 中**所有用户、所有会话逐字相同**的部分。抽 ≥3 个不同用户、不同日期的会话做逐字 diff,公共部分即 A2;**diff 出来的差异片段要单独列出**(这是"伪静态"体检:日期、用户名、随机排序等) | 总长(已知 25929)、**内部构成拆分**(角色设定 / 原则 / 输出规范 / 安全合规 / few-shot / 其他,各多长)、diff 差异片段清单 |
| A3 | 产品级 | **预置高频 skill desc** | SP 固定之后 | 目标态新增,现网无对应。需要 skill 调用频次数据来定 N | ① 近 30 天 skill 调用频次分布(按 skill 排序的累计覆盖率:top-5 / 10 / 20 / 50 各覆盖多少调用);② 单条 skill desc 渲染后长度分布;③ 当前 skill search 的 top-1 / top-3 命中率(若有) |
| B1 | 会话级 | **SP·会话级块** | SP 尾部 | 现网 SP 中**同一会话内不变、跨用户不同**的部分(user.md 等),加上目标态要放进来的记忆快照、已安装应用列表 | user.md(已知 313);记忆快照长度分布;已安装应用列表(应用个数分布 × 每条渲染后长度 → 总长分布);**会话中途是否会更新**(若会,频率) |
| B2 | 会话级 | **历史 10 轮 UAT** | UAT 头部 | 会话创建时从上次会话载入的最近 10 轮完整 U/A/T。识别:会话第一次调用的 messages 数组中、本会话 U1 之前的全部消息 | 总长分布(均值 / p50 / p90);其中 U / A / T 各占多少;实际轮数分布(不足 10 轮的会话占比);**是否完整 U/A/T、T 是否有单独裁剪** |
| C1 | query 级 | **U(query 本身)** | UAT 追加 | 当前每轮 user 消息(不含注入) | 长度分布 |
| C2 | query 级 | **动态上下文(当前在 SP)** | 当前 SP 内每 Q 重写段;目标下沉进 U | 相邻两个 Q 的 SP 逐字 diff,变化片段即 C2。拆成 available skills / memory / device_info 三项 | 三项各自长度分布(已有粗值 skills 2877–3565 / device 259–310 / memory 假设 1K,需 memory 实测);**每轮相对上轮真正变化的比例**(决定目标态增量注入能压到多少) |
| D1 | loop 级 | **A(assistant,含 tool call)** | UAT 追加 | 每次 loop 迭代的 assistant 消息 | 长度分布;若含 reasoning,注明模板是否剔除历史 reasoning(见 06 §3) |
| D2 | loop 级 | **T(tool result)** | UAT 追加 | 每次 loop 迭代的 tool 消息 | 按类型分:skill 正文 load / 普通工具结果 / webSearch,各自长度分布(已有粗值 5K / 3K / 10K) |
| D3 | loop 级 | **最终回复 answer** | UAT 追加 | 每个 Q 最后一次 assistant 消息 | 长度分布(已有粗值 ~0.5K) |

## 3. 会话形态与引擎侧(用于 P3–P9)

| # | 数据 | 用途 |
| --- | --- | --- |
| E1 | 每会话 Q 数分布(已有:1 轮 51% / 2 轮 20% / 3 轮 9% / 4 轮 5.5% / 5 轮 3.5% / 6 轮+ 11%);6Q+ 的均值 | P7 分桶 |
| E2 | 每 Q 的调用次数分布(loop 深度),按业务类型(问答 / 闲聊 / 任务) | P4–P6 |
| E3 | 业务占比(已有:问答 47 / 闲聊 28 / 任务 25) | P4–P7 |
| E4 | 每次调用输入 token 分布(已有均值 38.5K) | 校验 |
| E5 | 引擎是否返回 `cached_tokens`;若有,每调用命中量分布,按"会话首调 / Q 首调 / loop"三类拆开 | P3 校准、P8 |
| E6 | prefill 吞吐(token/s,目标机型与并发下) | P9 时延换算 |
| E7 | 会话创建 → 用户开口的平均间隔;ASR 识别平均时长 | P9 两次预热的可用窗口 |
| E8 | 同一用户相邻两次会话的平均间隔;缓存驻留超时配置 | P9 是否需要第二次预热 |

## 4. 已有值(沿用,不必重测)

| 模块 | 值 | 来源 |
| --- | --- | --- |
| SP 固定总长 | 25929 | v1 P1 实测 |
| user.md | 313 | v1 P1 实测 |
| available skills(动态) | 2877–3565 | v1 P1 实测 |
| device_info(动态) | 259–310 | v1 P1 实测 |
| Tools 块(当前) | 4413 | v1 P1 实测 |
| history 样本(含本会话) | 7184–13453 | v1 P1 实测,需按 B2 / 本会话拆开 |
| 每次调用输入均值 | 38.5K | 实测 |
| 轮数分布 / 业务占比 | 见 E1 / E3 | 实测 |

## 5. 填写模板

```json
{
  "meta": { "tokenizer": "", "sample_window": "", "num_sessions": 0, "num_calls": 0 },

  "A1_tools": {
    "current": { "total": 4413, "tools": [ { "name": "", "tokens": 0 } ],
                 "variants_by_rom": false, "variants": [ { "desc": "", "share": 0.0, "total": 0 } ] },
    "target":  { "total": 0, "tools": [ { "name": "", "tokens": 0 } ], "note": "元工具集若未定,留空" }
  },
  "A2_sp_fixed": {
    "total": 25929,
    "breakdown": { "role": 0, "principles": 0, "output_format": 0, "safety": 0, "few_shot": 0, "other": 0 },
    "diff_fragments": [ { "what": "如日期/用户名/随机排序", "tokens": 0 } ]
  },
  "A3_preset_skills": {
    "coverage": { "top5": 0.0, "top10": 0.0, "top20": 0.0, "top50": 0.0 },
    "desc_tokens": { "mean": 0, "p50": 0, "p90": 0 },
    "search_hit": { "top1": 0.0, "top3": 0.0 }
  },
  "B1_session_sp": {
    "user_md": 313,
    "memory_snapshot": { "mean": 0, "p50": 0, "p90": 0 },
    "installed_apps": { "count": { "mean": 0, "p90": 0 }, "tokens": { "mean": 0, "p90": 0 } },
    "changes_within_session": false, "note": ""
  },
  "B2_history_10": {
    "total": { "mean": 0, "p50": 0, "p90": 0 },
    "by_role": { "user": 0, "assistant": 0, "tool": 0 },
    "rounds": { "mean": 0, "lt10_share": 0.0 },
    "complete_uat": true, "tool_result_trimmed": false, "note": ""
  },
  "C1_user_query": { "mean": 0, "p50": 0, "p90": 0 },
  "C2_dynamic": {
    "skills": { "mean": 0, "p90": 0 }, "memory": { "mean": 0, "p90": 0 }, "device": { "mean": 0, "p90": 0 },
    "changed_ratio_vs_prev_q": { "skills": 0.0, "memory": 0.0, "device": 0.0 }
  },
  "D1_assistant": { "mean": 0, "p90": 0, "reasoning_dropped_by_template": true },
  "D2_tool_result": { "skill_load": { "mean": 0, "p90": 0 }, "tool": { "mean": 0, "p90": 0 }, "websearch": { "mean": 0, "p90": 0 } },
  "D3_answer": { "mean": 0, "p90": 0 },

  "E_session": {
    "q_per_session": { "1": 0.51, "2": 0.20, "3": 0.09, "4": 0.055, "5": 0.035, "6+": 0.11, "6+_mean": 0 },
    "calls_per_q": { "qa": 0, "chat": 0, "task": 0 },
    "biz_share": { "qa": 0.47, "chat": 0.28, "task": 0.25 },
    "input_per_call": { "mean": 38500, "p50": 0, "p90": 0 }
  },
  "E_engine": {
    "cached_tokens_available": false,
    "cached_by_call_type": { "session_first": 0, "q_first": 0, "loop": 0 },
    "prefill_tps": 0,
    "session_create_to_speak_ms": 0, "asr_ms": 0,
    "inter_session_gap_min": 0, "cache_ttl_min": 0
  }
}
```

## 6. 优先级

- **P0(阻塞 P1 填数)**:A1 当前工具清单与是否分变体、A2 内部构成与 diff 片段、B1、B2。
- **P1(阻塞 P3–P7 重算)**:A1 目标 Tools 长度、C2 变化比例、D2 分类型、E2、E5。
- **P2(P9 与附录)**:A3、E6–E8。
