# KV Cache 与 Agent 上下文工程讨论

这是一个讨论用目录，用于沉淀关于大模型 KV cache 命中率与 agent 上下文组织的设计讨论。
内容随讨论持续迭代。

## 目录（当前版本）

| 文档 | 内容 |
| --- | --- |
| [01-问题定义.md](./01-问题定义.md) | 当前系统面临的上下文组织问题（UAT 消息流、动态 skill 等） |
| [02-上下文分层与排布.md](./02-上下文分层与排布.md) | 按变化频率分层的排布方案与"渐进式加载"设计 |
| [03-压缩策略.md](./03-压缩策略.md) | 超长上下文"从远到近"压缩的缓存代价分析与改进 |
| [04-开放问题.md](./04-开放问题.md) | 待讨论、待确认的问题清单 |
| [05-渐进式加载详细设计.md](./05-渐进式加载详细设计.md) | L2 内容随 user message 结构化注入的详细方案（消息规范、去重、压缩交互） |
| [06-模型侧拼接细节.md](./06-模型侧拼接细节.md) | DeepSeek V4 / GLM 5.2 的 tools 拼接位置、defer_loading、reasoning 剔除与验证脚本 |
| [07-分代冻结详解.md](./07-分代冻结详解.md) | 分代冻结机制展开：合并式 vs 堆叠式、时间线算账、字节级冻结的工程陷阱 |
| [08-量化收益评估.md](./08-量化收益评估.md) | 基于现网基线（输入 37K / 命中 25K / 66%）的收益估算与敏感性分析 |
| [09-业界Agent-harness缓存实践洞察.md](./09-业界Agent-harness缓存实践洞察.md) | Manus / Claude Code / Anthropic 平台 / Mooncake 的缓存优化实践深度调研与借鉴清单 |
| [10-细化统计方案与分场景收益.md](./10-细化统计方案与分场景收益.md) | 长期度量体系:请求输入四分解、命中率/结构上限/达成率三层指标、单轮对话/单轮loop/多轮对话分场景收益口径、埋点与三步落地 |
| [11-收益闭环与统计口径-汇报版.md](./11-收益闭环与统计口径-汇报版.md) | 给领导闭环的版本(无驻留假设):各层长度盘点、多轮对话逐请求账目(现状162K vs 目标45K)、与现网基线互验(-75%/-56%)、两个计数器+验收等式的统计闭环、30秒话术 |
| [12-上下文长度实测需求.md](./12-上下文长度实测需求.md) | 交给内部模型的测量需求:模块定义与识别方法 + token 统计口径 + JSON 输出 schema,用于替换假设值 |
| [13-汇报大纲.md](./13-汇报大纲.md) | **v2 整合汇报大纲**:P1 组成对比(Q1–Q4,loop 只演示一次)→ P2 该命中/命中率/推理长度 + 三方案(双次预热、高频 skill 预置、动态只更新变化)→ P3–P5 闲聊/问答/任务 → P6 轮数×业务加权 → P7 自动化统计与分析 → 附录 |
| [15-上下文模块与长度基线.md](./15-上下文模块与长度基线.md) | 汇报版 v1.4 的数据底稿(= v1.4 P1):11 个上下文模块 M1–M11 / K1–K4 / R1–R6。**已被 16 / 17 / 18 取代**,只服务留档的 v1.4 / 升级版 v1–v2 幻灯片 |
| [16-数据基线-优化后.md](./16-数据基线-优化后.md) | **当前工作基线 · 优化后**(= v4.3 P1 右表):文档四列(序号 · 名称 · 解释 · 长度),幻灯片取前三列,行序 = 物理序,长度全为确定数。Tools 5.9 → System Prompt 固定 28.9(高频 skill 不进)→ 会话级 1.5(user.md 0.5 + device_info 0.5 + memory.md 0.5)→ 历史 **4**(上一会话对话历史,会话创建时载入、大概率不在缓存)→ **U 动态 3.5**(available skills 3 + 动态记忆 / 知识经验 0.3 + 前台应用 / 小艺状态 / 位置 0.05)→ U query 0.1 → A tool call 0.2 → T(检索 10 / skill 5 / 正常工具 2.5)→ A answer 0.25;**检索 / skill preload 以伪造 A/T 随 U 进入首调、不占模型调用**,问答 1 次调用、任务 2 次、闲聊 1 次;与 17 逐项同长 |
| [17-数据基线-优化前.md](./17-数据基线-优化前.md) | **当前工作基线 · 优化前**(= v4.3 P1 左表):同样四列、单独成篇。System Prompt(固定 → 动态 → 会话级)→ Tools → 历史 → U query → A tool call → T → A answer;长度与 16 逐项相同,差别只在位置与缓存命运 |
| [18-三页汇报版账目与公式.md](./18-三页汇报版账目与公式.md) | **v4.3 P2 / P3 的数据底稿**:§00 **三个汇总维度**(第 n 轮 / L 轮的 session / 全量;命中率定义唯一、只是汇总范围不同;两层权重:业务 47 / 28 / 25 → session 长度分布);§0 调用链(问答 1 次 / 任务 2 次 / 闲聊 1 次,检索 / skill 以伪造 A/T 进首调)与两个必算口径(首调必算 3.85K 闲聊基准、问答 +10.2 / 任务 +5.2,决定 TTFT;整轮实算 10.6K 决定成本);P2 底部两行(白算 88.6K → 0、必算 38.8K 两边相同、实算 127K → 38.8K −70%、命中率全业务加权 55% → 86%);5 次调用演示会话逐调用账(含每次调用命中率,加权 58% → 88%);实算 = 内部口径"硬算";轮数公式 ΔS(n≥2) = 11.4 + 7.1(n−1)、C / T / η / 每轮命中率合表(Q1 −27% … Q6 −82%,命中 65 → 75% … 47 → 92%,12 轮 95%);**命中率相对 v3.5 下降的原因**(问答不再有第二次高命中调用,口径更接近实测约 60%);首调 TTFT 表(Q1 690 → 420 ms、Q2–Q12 1.0–4.3 s → 180 ms,预热后统一 170 ms)+ 业务必带(问答 +470 ms / 任务 +240 ms);**§4 session 口径**:实测 8 万条 session 分布表(均 1.35 轮,82.6% 只有 1 轮;日均 2.52 轮 ≈ 1.9 session / 日)、**裁剪规则**(第 10 轮触发,本会话史累积封顶 9 轮;v4.0 的"续轮不累积"两点公式已废弃)、逐轮省额、"一条 session 省多少"表(1 轮 −27% … 10 轮 −79%)、按 session 长度分桶(**加权 token −45% / 成本 −35% / 命中 62 → 80%**);成本折算 ≈0.79 × token;三句话改 session 口径(17% 的 2 轮+ session 吃掉 49% 算力);口径边界改写(日口径不作上限) |
| [kv cache方案（汇报版）.v4.3.slides.html](./kv%20cache方案（汇报版）.v4.3.slides.html) | **最终汇报版(当前:v4.3,3 页;PPT v1 对应 v4.1 布局、数字相同)**。**v4.3 要点(P3 对称化)**:左右两列同构——顶部重点条(左:目标每轮实算 = 首调必算 + 第二次调用,闲聊 3.9 / 任务 11.8 / 问答 14.1 → 加权 10.6K,当前 = 10.6 + 白算 11.4 + 7.1×(n−1);右:全量加权横带 −45% / −35% / 62 → 80%)→ 图(左:第 1–6 轮实算折线;右:1–10 轮 session 省多少柱图,放大、去命中率折线)→ 7 行表(左:逐轮账第 1 / 2 / 3 / 4 / 5 / 6 / 10 轮;右:分桶 1–10 轮+ 的 session,全量行挪进横带);**结论四格横跨底部**;ΔS 公式 / 沉淀速率构成 / 两个口径整段撤出页面(留 18),页脚删命中率定义段。**v4.2 要点(P3 按维度分栏)**:左列 **"第 n 轮"** 单轮视角——公式块 + 第 1–6 轮实算折线(压矮)+ **逐轮账表**(第 1 / 2 / 3 / 4 / 6 / 10 轮:当前 / 目标实算、η、该轮命中率、首调 TTFT 当前 → 目标 → 预热后;原 TTFT 卡并入);右列 **"L 轮的 session"** 累计视角——分桶表首列写 "1 轮的 session …"、命中率列头 "session 累计命中率"、"一条 session 省多少"柱图挪到此列并放高 + **"全量"** 加权横带;"全业务加权"统一改称"全量加权"(每个数都已按业务加权,全量 = 再按 session 长度分布加权),标题与 P2 尺子④同步;页脚写命中率唯一定义与两层权重;结论卡左列(迷你横条 / 大数字)统一左对齐。**v4.1 要点**:撤销 v4.0 为对齐预期值人为设的"续轮不累积"两点公式,改为**第 10 轮触发上下文裁剪**(本会话史累积封顶 9 轮),其余按实测 session 分布正常累加——**token −45%(成本 −35%),cache 命中率 62% → 80%**;P3 公式带换成**"一条 session 省多少"柱状图**(1–10 轮当前 / 目标合计实算并排柱,柱顶标降幅,虚线标目标命中率,10 轮标裁剪)+ 一行加权结论;上图去掉每轮命中率子图(信息已在分桶表与柱图);分桶表桶名带"轮";P1 换算行简化为 "1.35 轮 / session · 2.52 轮 / 日";P2 成本尺子标"本页示例 4 轮 session"并写业务加权 4 轮 −63% / 全业务 −45%;结论卡改名"结论",四行用两列 grid 左列同宽对齐,去掉"第一步已上线"小字。**v4.0 要点(口径从"日"切到 "session")**:P1 右下改为 **session 轮数分布**(实测 8 万条,8 组横向柱状图,条长按平方根缩放让 1 轮 82.6% 不压扁其余,右侧数字带条数;下方高亮一行"1.35 轮 / session · 日均 2.52 轮 ≈ 1.9 session / 日",日口径只在此提一次);P3 标题改 **"全业务加权 token −39%(成本 −31%),cache 命中率 64% → 78%"**;分桶表改 7 组 session 桶(1Q 82.6% … 10Q+ 0.4%,逐桶无裁剪),加权行用**均轮数公式**(首轮 / 续轮两点、续轮不累积 = 长 session 上下文裁剪折扣),表下**高亮公式带** S(L) = 1 − [T₁ + (L−1)T₂] / [C₁ + (L−1)C₂]、H(L) 同法、成本 ≈ 0.79 × token,并给 1.5 / 2 / 3 轮的放大值(−42 / −48 / −54%);三句话①改 "17% 的 2 轮+ session 吃掉 49% 算力、新架构上线后 session 变长收益放大",②加 "83% 的 session 只有 1 轮、预热是短 session 主要时延收益",③下一步加 "历史 4K 整段载入改摘要";TTFT 表精简为 Q1 / Q2 / Q12;P2 命中率尺子改 session 口径 64% → 78%;页脚写裁剪口径(无裁剪累加 −45% 偏理想)、成本折算系数、当前命中率 64% 与实测 60% 相符;不再出现"日口径上限"。**v3.6.1**:P2 账目逻辑第 ② 格注明实算的内部口径叫"硬算"(引擎逐请求实报);逐次调用矩阵每行账目末尾加该次调用的 cache 命中率 chip(当前 53% → 46% 逐行下滑、目标 64% → 95% 逐行上升),行标下写演示会话 5 次加权 58% / 88%,与底部尺子的全业务加权 55% → 86% 分开。**v3.6 要点**:调用链按实际改——检索结果 / preload 的 skill 以**伪造 A(tool call)/ T(result)** 随 U 进入首调输入、不占模型调用,问答 1 次调用、任务 2 次(skill preload → tool call → 工具 → answer)、闲聊 1 次;**修 P2 当前侧矩阵 bug**(v3.5 及之前前缀第 2 / 3 列动态 / 会话级格色写反,Q2–Q4 首调的动态格现在正确为红色原位重写);矩阵改 5 行 × 10 UAT 列(A₁T₁ 检索 · A₂T₂ skill · A₂′T₂′ 工具);首调必算按业务三档 3.9 / 9.1 / 14.1K,整轮实算 10.6K;cache 命中率全业务加权 **55% → 86%**(6 轮 92%、12 轮 95%;命中率图纵轴 40–100%),成本 −65% 不变;**P3 分桶表"占总节省"换成"cache 命中率 当前 → 目标"**;TTFT 表脚加业务必带(问答 +470 ms / 任务 +240 ms,预热不覆盖);三句话加第四条**"效果不变"**(第一步已上线、对当前对话效果影响不大,定性);页脚加实测对照占位(当前约 60% / 12K,目标 90% / 7K)。全稿 "SP" 改写为 "System Prompt"。**P1 数据基线**:左右两张三列表(序号 · 模块 · 长度),history 4K、UAT-U 动态 3.5K,会话级与动态的说明写出具体构成(user.md / device_info / memory.md 各 0.5;available skills 3 + 动态记忆 0.3 + 前台应用 / 小艺状态 / 位置 0.05),history 注明"会话创建时载入、大概率不在缓存 → 会话首调必算、预热可覆盖";右栏业务占比表 + **会话轮数分布改为单列 16 行横向柱状图**(条长线性、最小 2px,读数看右侧数字;蓝 1–5Q / 紫 6–10Q / 深紫 11Q+ 与 P3 分桶同色;脚注只写 ≥11 轮按 12 轮计,不写归一化)。**P2**:结构条 + 三步改造一行 + 6 行逐次调用矩阵;底部两行账目逻辑 / 目标与衡量,目标② 改为"整轮必要新增"并写明**首调必算 3.9K(决定 TTFT)/ 整轮实算 10.6K(决定成本)两个口径**;演示会话 127K → 38.5K −70%、白算 88.2K;**命中率尺子改用全业务加权 67% → 91%**,与 P3 标题同一个数。**P3**:公式块加一行"首调必算 3.9 + 加权 loop 6.7 = 整轮实算 10.6";左列只剩公式 + Q1–Q6 双图(η −27 / −64 / −76 / −82%,命中率当前 75 → 61%、目标 83 → 95%);右列三块——分桶表(1Q–5Q / 6–10Q / 11Q+,加权 −65%)、**首调 TTFT 预估表**(Q1 / Q2 / Q6 / Q12 × 当前 / 目标 / 目标 + 预热,预热列统一 170 ms;表脚写预热定义与收益 −40% / 代价 +10% 算力 + TTL)、**三句话压成"大数字 + 一句"**(第一句左侧为**"会话数 10% / 算力 54%"两行迷你横条**,蓝短红长,一眼看出少数会话吃掉多数算力;贡献 65% 节省 / 3.9K:会话级可提前算、预热后所有轮次 ≈170 ms / 白算 → 0:验收线,整轮必算 10.6K 是下一步,skills 升会话级则首调必算 → 0.9K);标题"全业务加权实算 −65%,cache 命中率 67% → 91%"。所有数字由页内 `BASELINE` 算出并 `data-b` 绑定;`check_slides.py` 三视口 3 页全通过。v4.2 / v4.1 / v4.0 / v3.6.1 / v3.6 / v3.5 / v3.4 / v3.3 / v3.2 / v3.1 / v3、升级版 v1 / v2、v1.4 及更早留档 history/ |
| [kv cache方案（汇报版）.v1.pptx](./kv%20cache方案（汇报版）.v1.pptx) · [make_report_pptx.py](./make_report_pptx.py) | **真实 PPT 版(v1,16:9,3 页)**,由 `python3 make_report_pptx.py` 从最新 HTML 汇报版的 `BASELINE` 用 node 算出全部数字后排版(python-pptx;**当前 PPT 的 P3 仍是 v4.1 布局**,v4.2 的单轮 / session 分栏待下一版同步),风格与 HTML 一致(同色板、卡片、级别 chip、位置变了的行蓝底);P1 两张三列基线表 + 业务占比 + session 分布横向柱(平方根缩放)+ 均轮数高亮行;P2 两条结构条 + 当前 / 目标两张 5 行逐次调用矩阵(格色 + 逐行账目与命中率)+ 三格账目逻辑 + 四把尺子;P3 公式块 + Q1–Q6 每轮实算折线(原生图表,类目轴带 η)+ "一条 session 省多少"簇状柱(原生图表,柱顶降幅 / 目标命中率)+ 加权结论横带 + 分桶表 + TTFT 表 + 四条结论。已用 LibreOffice 无头渲染三页核对不溢出 / 不遮盖;HTML 改基线后重跑脚本即同步 |
| history/kv cache方案（汇报版）.v4.2.slides.html | v4.2(3 页,**已被 v4.3 取代**):首次按第 n 轮 / L 轮 session / 全量三维度分栏,但左右不对称(左折线 + 逐轮表,右分桶表 + 柱图 + 横带 + 结论),页面保留整段 ΔS 公式与"命中率定义"页脚,柱图带命中率折线略重叠 |
| history/kv cache方案（汇报版）.v4.1.slides.html | v4.1(3 页,**已被 v4.2 取代**;PPT v1 按此版布局):数字与 v4.2 相同;P3 左列为公式 + 折线 + "一条 session 省多少"柱图,右列为分桶表 + TTFT 表 + 结论,单轮与 session 两个维度混排、"全业务"叫法未改、结论卡左列未对齐 |
| history/kv cache方案（汇报版）.v4.0.slides.html | v4.0(3 页,**已被 v4.1 取代**):首次切到 session 口径,但加权用"续轮不累积"两点公式(token −39% / 成本 −31% / 命中 64 → 78%),该口径为对齐预期值人为设定、已废弃;P3 有 S(L) / H(L) 公式带,结论卡左列未对齐 |
| history/kv cache方案（汇报版）.v3.6.1.slides.html | v3.6.1(3 页,**已被 v4.0 取代**):轮数分布为**日口径**(用户 × 日,16 桶,≥11 轮按 12 计),全业务加权 −65% / 命中率 55% → 86% 为日口径且逐桶无裁剪累加;P1 柱状图线性;P3 无均轮数公式带、TTFT 表 4 行 |
| history/kv cache方案（汇报版）.v3.6.slides.html | v3.6(3 页,**已被 v3.6.1 取代**):数据与 v3.6.1 相同;P2 矩阵右列无逐行命中率、概念解释未注"硬算"别名 |
| history/kv cache方案（汇报版）.v3.5.slides.html | v3.5(3 页,**已被 v3.6 取代**):调用链为旧口径(问答 / 任务各 2 次调用,webSearch / SkillLoad 走 loop);P2 当前侧矩阵前缀第 2 / 3 列格色写反(动态格未显示红色);命中率 67% → 91%;分桶表最后一列为"占总节省";三句话无效果条 |
| history/kv cache方案（汇报版）.v3.4.slides.html | v3.4(3 页,**已被 v3.5 取代**):数据与 v3.5 相同;P1 柱状图脚注写归一化那句;P2 命中率尺子为演示会话 63% → 89% 再挂全业务加权 91% 两个数;P3 第一句左侧为 "10% → 54%" 箭头写法(易被读成从 10 涨到 54) |
| history/kv cache方案（汇报版）.v3.3.slides.html | v3.3(3 页,**已被 v3.4 取代**):history 7K、动态 3K、写 "SP";P1 右栏轮数分布为两列表 + 分布带;P3 TTFT 为左下一条横条、三句话长文案,首调必算 3.4 与整轮 10.1 并列未标口径;加权 −66%、命中率 66% → 91% |
| history/kv cache方案（汇报版）.v3.2.slides.html | v3.2(3 页,**已被 v3.3 取代**):query 0.03K;P1 右栏为"两表的差别"三条 + 6 桶轮数分布条(6Q+ 合并 11% 按 6 轮);P2 底部为四把尺子 + 五格恒等式;P3 曲线 Q1–Q8(Q8 命中率标 96%,与"上限 95%"口径不一)、分桶 6Q+ 按 6 轮加权 −59%、命中率 69% → 89%、无 TTFT 预估 |
| history/kv cache方案（汇报版）.v3.1.slides.html | v3.1(3 页,**已被 v3.2 取代**):P1 / P2 与 v3.2 相同;P3 只有实算单图,η 全点标注且 Q8 被切,Q1 混写首调 −34% 与整轮 −24% 两种分母,"命中率上限 93%"为 8 轮单会话口径 |
| history/kv cache方案（汇报版）.v3.slides.html | v3(3 页,**已被 v3.1 取代**):P1 为结构条 + 六列对比表 + 零件 chip + 恒等式,P2 为四张 KPI + 三步 + 五次调用横向堆叠柱,P3 与 v3.1 同源但 6Q+ 按 8 轮(加权 −63%)。数据口径与 v3.1 相同 |
| [模块长度与变化行为-M1-M9.md](./模块长度与变化行为-M1-M9.md) | 工作底稿：M1–M9 的长度 / 变化行为(当前 → 目标) / 状态备注（粘贴稿拍平）；[tsv](./模块长度与变化行为-M1-M9.tsv) 为机器可读副本。**M5–M8 编号与 15 不一致**；长度与变化行为已吸收进 16 / 17 |
| [基线模板.v2-0921实测.md](./基线模板.v2-0921实测.md) | **基线模板 v2（0921 实测）**：7 个 promptKvCache trace（Q1×3 + Q10×2 + Q11×2）按物理顺序拆段；M1–M9 定义（M3 未实现、M7=developer 且每 Q 变、为 cache 断点）；chars 实测表 + M7a/M7b；token 填写区待 DS-V4 分词校准。**注意**：它测的是现网中间态（动态已挪为每 Q 一条 developer 消息、仍排在历史之前），不是 17 的优化前，也不是 16 的目标态 |
| [14-上下文组成实测数据需求.md](./14-上下文组成实测数据需求.md) | 填充 P1 虚框与 P2–P7 参数的数据需求(12 的续篇,按四级分级重定义模块):A 产品级(Tools 清单与变体 / SP 固定构成与伪静态 diff / 预置 skill 覆盖率)、B 会话级(SP 块 / 历史 10 轮)、C/D query 与 loop 级各消息长度、E 会话形态与引擎侧(cached_tokens、prefill 吞吐、ASR 时长;**E1 已收 8 万条 session 轮数分布**,区分日口径 / session 口径);统一口径 + JSON 填写模板 + 优先级 |
| [token_breakdown.csv](./token_breakdown.csv) | **14 的逐调用实测回填**(73 行):before/after 对照,字段对齐 14 的 A2/B1/C2/A1/C1/UAT/input/cache_read;覆盖 task/qa/chat(Q1–Q6、Q8–Q11),模型含 GLM-5 与 DeepSeek-V4 |
| [prompt段内容变化对照.md](./prompt段内容变化对照.md) | **优化前后 prompt 各段内容对照**(Q8 闲聊首调,同模型 DS-V4-Flash-0731):逐段原来/现在/为何变 + 变化类型(不变/更新/迁移/填充) + 量化(SP −7.6%、动态内容移出 SP、Q2 起 miss 钉在 ~8K);配套 slides 数据底稿 |
| [上下文组成对比.v1.2.slides.html](./上下文组成对比.v1.2.slides.html) | **v2 汇报 P1(当前 v1.2)**:目标态 Tools → SP → UAT;上半四级结构条;下半矩阵改为 4 个 Q、loop 只演示一次(Q1 调②),Q2–Q4 只画首调以体现多轮收益。v1 / v1.1 留档 history/ |
| [尺子与关键方案.v1.slides.html](./尺子与关键方案.v1.slides.html) | **v2 汇报 P2**:该命中(产品级/会话级/轮次) + 命中率 + 推理长度通式;三个方案——A 会话创建+query/ASR 双次预热、B 高频 skill 预置、C 动态只更新变化 |
| [自动化统计与分析.v1.slides.html](./自动化统计与分析.v1.slides.html) | **v2 汇报 P7**:点位(含会话首调/预热标签、目标 bounds Tools 最前) + 断点拆段 + 三张报表(命中率/miss/分桶) + 报警;旧版统计 v1.2 仍配合收益闭环 v7.2 |
| history/kv cache方案（汇报版·升级版）.v2.slides.html | 升级版 v2(12 页 + 附录 4 页,**已被 v3 系列取代,移入 history/**;数据仍是 v1.4 口径,含 skill 预置与 ≤2K 假设)。在 v1 之上把 archify 的**画图与动态能力**接进 deck(配色 / 版式不动):**新增 P2 全景架构**(`architecture`:9 组件 · 2 边界 · 10 连线——版本发布 / 用户私域 / 会话历史 / 检索 四路来源 → Agent 应用 → 推理引擎 ↔ KV 缓存 → 工具,region "缓存实例 · 会话粘性"、security-group "用户私域",三章:三级来源 / 一次调用 / 工具往返)、**新增 P5 四步改造路线**(`workflow` v2:产品级 / 轮次 / 会话级三条泳道 + "不进账本"例外道,主线 = 四步,每条边写它让账目里哪一段红转绿,四章 = 四步)、**新增 P11 统计管线**(`dataflow`:来源 / 明细 / 归因 / 聚合 / 输出五阶段 7 节点,三章 = 采集 / 归因 / 报表与报警;P10 的 chip 流程改为指向此页),**附录 B / C 的两张 sequence 加 `animation: trace` + `views`**,按段分章。五张图全部 `validate --quality showcase` 9/9 · 0 error · 0 warning 后 `deliver`,由 [slide-style/inline_archify.py](../slide-style/inline_archify.py) 抽 SVG 内联并**保留 data-node-id / data-edge-id / data-animate / --step 钩子**;deck 侧 ~120 行脚本驱动:进页 trace 一遍(边按 authored 顺序流动、节点脉冲),`→` / `←` 先走图内章节再翻页(非焦点节点压暗、章内边按序流动、时序图非当前段压暗、底部章节栏带说明),`P` 自动播放(3.2 s / 章),`↑` `↓` 直接翻页,深链 `#p=N&c=K`,`hashchange` 可从外部驱动。旧 P2–P9 顺延为 P3 / P4 / P6–P10 / P12,全稿页码引用同步改。`check_slides.py` 三视口 16 页全通过。升级版 v1 留档 history/ |
| history/kv cache方案（汇报版·升级版）.v1.slides.html | 升级版 v1(9 页 + 附录 4 页,**已被 v2 取代**)。内容与数据 = v1.4(`BASELINE` / `SESSIONS` 数据绑定不变),按 [docs/slide-style/01-archify借鉴分析.md](../slide-style/01-archify借鉴分析.md) 的九条借鉴做样式与交付升级:① 色块改"低透明填充 + 同色描边 + 描边色文字"(结构条 / 矩阵 / 账目条 / 级别 chip / P8 归因条),去实心底与 text-shadow;② 层级色只表类型,状态只用命中 / 白算 / 新增三色 + 斜纹(首次载入 / 异常)+ 虚线(下沉后的动态);③ 页头改三段 eyebrow(页码 · 章节)+ 短标题 + 一句 lede,口径下沉页脚,右上角实测 / 推演 / archify badge;④ 删掉 `.card{overflow:hidden}` 裁切,P3 由 denser 降为 dense、P2 矩阵行高与段块文案收紧,`check_slides.py` 三视口 13 页全通过;⑤ 图例由脚本按出现过的类型自动生成;⑥ `#p=N` 深链、`[` `]` 跨章节、`F` 演示态、`T` 深浅色、`S` classic / editorial 预设(暖纸 + 衬线标题);⑦ **附录 B 的"两次 prefill"时间线换成 archify `sequence`**(4 参与者 · 3 段 · 10 条消息),**新增附录 C "一个 Q 的两次模型调用:缓存在哪儿命中、在哪儿断"**(5 参与者 · 2 段 · 12 条消息,右侧四张说明卡对应 P4–P6 的读法),两图均 `validate --quality showcase` 9/9 · 0 error · 0 warning 后内联 SVG。v1.4 及更早留档 history/ |
| [archify/](./archify/) | 升级版 v2(已留档)五张图的 archify Typed JSON 源与 deliver 出的独立 HTML(答问时打开:`R` 探路、聚焦节点看 Upstream / Downstream、`P` 播放章节、`?theme=light&present=1`):`P2-全景架构.architecture`、`P4-四步改造.workflow`(schema v2)、`P9-统计管线.dataflow`、`附录B-会话级预热.sequence`、`附录C-一个Q的两次调用.sequence`(文件名里的 P 编号是写图时的页位,deck 里实际落在 P2 / P5 / P11 / 附录 B / 附录 C)。改图先改 JSON,`validate <type> <json> --quality showcase --json` 9/9 后 `deliver`,再 `python3 ../slide-style/inline_archify.py <json> <html> --suffix xx` 抽 SVG + 章节内联(步骤见 slide-style/01 第四、六节) |
| history/kv cache方案（汇报版）.v1.4.slides.html | 升级版的内容来源(9 页 + 附录 3 页):v1.4 新增 **P1 数据基线页**(M1–M11 模块 × 当前 / 目标长度 × 状态,K1–K4 常量,R1–R6 规则;与 15 同源),全稿长度 / 目标 miss / 节省率由页内 `BASELINE` + `SESSIONS` 对象计算并 `data-b` / `data-calc` 绑定,改基线即全稿同步;P3 "三方案"改为与 P2 四句同序的**四步改造**(① 动态下沉已上线 ② Tools 前移 ③ skill 预置 ④ 只更新变化,预热不进清单),device 统一归会话级,动态基线统一 3.0K → ≤2K;P4–P6 账目页改为数据渲染(Q 边界按 U 列变化 K4,任务 / 问答各含 1 次 loop 全 miss、目标归零;三条实测会话合计 −73% / −79% / −82%),修掉标签重叠、条纹低对比、页脚与翻页键重合;**已被升级版 v1 取代,移入 history/**。v1.3 按 13 大纲 / v7.2 模板重排全稿——P1 组成对比(去预热口径;4Q 闲聊尺子改为 54K → ~10K,−81%)→ P2 两把尺子 + 三方案(去中间态列,改为"目标 / 目标的实测校核";方案重排为 A 高频 skill 预置、B 只更新变化、C 会话级预热放最后且不进账本)→ **P3 任务型 / P4 问答型 / P5 闲聊型三页逐 Q、逐调用账目(v7.2 版式,当前实测 vs 目标推演同位对照,每 Q 给省额与比例;三条实测会话合计 −73% / −80% / −81%)**→ P6 整体收益(对账表改为三类 Q 首调实测 vs v7.2 推演)→ P7 自动化统计(scheme 只保留当前 / 目标)→ P8 结论(KPI 改为当前 → 目标口径);"中间态"不再作为主线一列,已上线第一步的实测下沉为**附录 A1(各段内容对照)/ A2(问答 + 任务 9 次调用审计 + 对 P3–P5 目标列的三点校核)**,作为目标推演的证据;**附录 B 方案 C 会话级预热**(只影响首调 TTFT,三类会话首调 当前 / 目标 / 预热后 对照)。v1.2.1 修正 P1 矩阵语义;v1.2 重做 P1(两行结构条对位命名、三尺子对比条);v1.1 锁定目标 Tools = 全量 12 个预置、预置 skill 15 个 4.5K。替代 kvcache整体方案.v1;v1 / v1.1 / v1.2 / v1.2.1 / v1.3 留档 history/ |
| [kvcache整体方案.v1.slides.html](./kvcache整体方案.v1.slides.html) | 上一版整合汇报材料(9 页,已被"kv cache方案（汇报版）"系列取代,暂留作对照):P0 整体收益总览("白算的 token 才是成本",版式源自收益闭环 v4、数字为 v7.2 口径)+ P1–P7 收益闭环(v7.2 全量七页)+ P8 统计点位与自动化(v1.2 单页) |
| [KV-Cache收益闭环.v7.2.slides.html](./KV-Cache收益闭环.v7.2.slides.html) | 汇报幻灯片 v7.2(7 页 · 浅色 · 当前版本):①去掉 15K/60K 触顶限制(理想化假设,P1 注明与实测 15K 差异源于现网裁剪);②命名全篇统一 Q(P3/P4 行名、P6 分桶 1Q~6Q+);③新增 P5"按 Q 序号加权收益"页(Q1~Q5 逐位账目 + Qn 公式 ΔS(n)≈7K+6.1K×(n−1),目标态恒 ~7.5K/轮);④P2 备注无 skill 首轮(省同 7K,-58%,占比待回填);问答 Q3 40K→12K(-70%)、加权 -68%,任务加权 -69%,闲聊 -81%,混合整体 -69%(每会话 64.2K→19.6K),6Q+ 会话占 51% 算力贡献 58% 节省 |
| [KV-Cache统计点位与自动化.v1.2.slides.html](./KV-Cache统计点位与自动化.v1.2.slides.html) | 统计落地单页 PPT v1.2(配合收益闭环 v7.2,面向领导):左块点位表细化到字段级(类型+示例/枚举:bounds 分方案两种顺序、标签枚举 biz/call/scheme/q、⑥输出与时间注明回复由下一条明细②③自动覆盖);右块——怎么算(当前/目标双结构示意图,只标模块名不标数字,"改造=把断点 d 往后推")/ 怎么比(方案定义+四项关键对比表)/ 怎么跑守(管线+报警) |
| [上下文工程与KV-Cache优化.v8.slides.html](./上下文工程与KV-Cache优化.v8.slides.html) | 汇报用 HTML 幻灯片 v8（基于 v7：术语规范 query级/Tools块、目标页标题"挑战 90%+ 命中率"、删输入输出比卡、附录 D 增补"为何非 100%"口径说明） |
| [上下文工程与KV-Cache优化.pptx](./上下文工程与KV-Cache优化.pptx) | 真实 PPT（浅色系，2 页：现状与问题 / 目标方案），由 [make_pptx.py](./make_pptx.py) 生成 |

## 历史版本（[history/](./history/)）

被新版取代的幻灯片全部留档在 [history/](./history/) 子目录，共五个系列：

| 系列 | 留档版本 | 当前版本 |
| --- | --- | --- |
| KV-Cache收益闭环 | 初版、v2~v6、v6.1~v6.9、v7.0~v7.1 | v7.2 |
| KV-Cache统计点位与自动化 | 初版、v1、v1.1 | v1.2 |
| 上下文工程与KV-Cache优化 | 初版、v2~v7 | v8 |
| 上下文组成对比 | v1、v1.1 | v1.2 |
| kv cache方案（汇报版） | v1、v1.1、v1.2、v1.2.1、v1.3、v1.4、升级版 v1、升级版 v2、v3、v3.1、v3.2、v3.3、v3.4、v3.5、v3.6、v3.6.1、v4.0、v4.1、v4.2 | v4.3(3 页 + PPT v1(v4.1 布局):数据基线两张三列表 + 轮数横向柱状图 / 组成对比 + 账目逻辑与目标两行 / 轮数收益双图 + 分桶 + 首调 TTFT 表 + 三句话;System Prompt 全称,首调必算 / 整轮实算两口径,预热后统一 170 ms;命中率只报全业务加权 67% → 91%,长会话"会话数 10% / 算力 54%"迷你横条;调用链按伪造 A/T 实际口径、命中率 55% → 86%、分桶表带命中率列、四条结论含效果不变;实算注"硬算"别名,矩阵逐行命中率;**session 口径**:实测 8 万条分布、第 10 轮裁剪、实测分布加权 token −45% / 成本 −35% / 命中率 62 → 80%,"一条 session 省多少"柱图;P3 按第 n 轮 / L 轮 session 左右对称分栏 + 底部结论四格) |

## 背景共识

- KV cache 命中条件：**前缀逐 token 完全一致**，改动一个 token，其后全部失效。
- Agent loop 输入输出比悬殊（常见 100:1），缓存命中与否直接决定成本（约 10 倍差价）和 TTFT。
- 一切上下文组织决策，都要回答同一个问题：**这次改动会让缓存从哪个位置断掉。**
