# 08 场景全集与状态交互分层（统一状态模型 v2）

本章把 [06 章](./06-全双工输出状态盘点.md) 的"统一 10 态模型（讨论稿）"推进到 v2：
**从交互场景推导状态**（而非从直觉枚举），给出**完整的状态交互图及其四层分层**，
校订一份外部给出的事件词表，并落一个新需求——**外挂 ASR 的外部启动标识**。
配套幻灯片见 [全双工输出状态.slides.v2.html](./全双工输出状态.slides.v2.html)。

## 0. 结论摘要

1. **状态从场景来**：一轮交互切成 A 静默 / B 用户输入 / C 响应准备 / D 系统输出 五个阶段
   加 X 贯穿并发，共 34 个场景；场景里"系统必须在此刻做一个判定"的点才产生状态需求。
2. **三分类原则**：交互态（稳定、UI 可渲染）/ 判定窗口态（短暂、带计时器、结论出来就坍缩）/
   决策事件（瞬时、驱动转换）。状态图变复杂的根源就是把窗口和事件画成了节点。
3. **v1 → v2 的主要修订**：`THINKING/DELEGATING` 按"是否已出声"拆分——未出声更名
   **PREPARING**（话轮已接、还没开口，v1 的遗漏点：用户说完到系统首帧之间的时延都堆在这里），
   出声后的宣告等待并入 **HOLDING**；`RESUMING` 降级为 SPEAKING 上的瞬态自环；
   工具调用 / 委托大模型**不建交互态**，降级为决策事件。
4. **时延不是状态**：PREPARING / HOLDING 内部用 T0（无感）/ T1（出填充）/ T2（兜底）
   三阈值把连续时延切成可设计的体验分段。
5. **状态图分四层**：信号源 → 判定窗口 → 事件（控制面词表）→ 交互状态。
   外部给出的 7 项词表整体落在事件层，缺窗口层与状态层，且混入了一个状态（`listen`）。
6. **外挂 ASR 标识**挂在判定窗口的进出沿上：`asr_prearm / asr_start / asr_stop / asr_cancel`
   两段式信号，并泛化为 segment 级外部协处理触发。

## 1. 定义框架

### 1.1 三层定义（承接 06 章三层视角的工程化）

| 层 | 回答的问题 | 验收标准 |
| --- | --- | --- |
| 交互层 | 用户感知到系统在做什么 | 只描述可感知事实；状态锚定在播放事件与 VAD 事实上（LiveKit 的教训：`SPEAKING` 以首帧入播放通道为准，不以模型开始生成为准） |
| 模型输出字段层 | 模型每帧 / 每 chunk 输出什么 | 控制流 + 内容流；交互层状态可由字段 + 播放时间戳**纯函数推导**，不需要额外启发式 |
| 系统需求层 | 系统还额外要模型输出什么 | 工具 / 检索 / 委托大模型 / 外发标识所需的控制协议 |

### 1.2 三分类原则

| 类别 | 特征 | 在状态图里的画法 |
| --- | --- | --- |
| 交互态 | 稳定、UI 可渲染、可被产品文档引用 | 节点 |
| 判定窗口态 | 短暂、有超时、结论出来就坍缩到某个交互态 | 转换边上的"胶囊"标注，不占节点 |
| 决策事件 | 瞬时判定、驱动转换 | 边的标签 |

### 1.3 双通道输出协议（控制面 / 内容面）

```text
控制面：voice_intent / floor_intent / 打断与恢复 / 升级请求 / 外发标识（asr_*）
内容面：├─ 音频流（codec token）
        ├─ 文本流（转写 / 语义，超前音频）
        └─ 工具调用流（结构化参数）
```

硬约束：控制流与内容流**共享时间基准**（帧号 + `segment_id` + 毫秒偏移），
否则打断对账（truncate 到用户实际听到的位置）无法完成；控制信号放在**超前**的
流上，给系统留反应窗口。

## 2. 人机 vs 人人：六条差异及其逼出的机制

| # | 人人对话 | 人机对话 | 逼出的机制 |
| --- | --- | --- | --- |
| 1 | 没有"拒识"，受话人总有反馈 | 误唤醒、旁路收音、多人在场 | 拒识决策事件（respond / reject / defer），全双工下变为**持续判定** |
| 2 | 靠句法韵律预测话轮结束，接话间隙常为负 | 等 VAD 阈值，慢半拍且在换气口抢话 | 用户 `PAUSED`（停顿 ≠ 说完）、语义 endpoint、**预提交**（停顿期预生成不出声，即 Flux Eager 思路） |
| 3 | 听者"嗯 / 对"是自然伴随 | 每个"嗯"都可能触发 barge-in | 打断判定窗（backchannel / 真打断 / 噪声三分） |
| 4 | 被打断者说完当前小句优雅让出 | 立刻闭嘴生硬、不理霸道 | `YIELDING` 显式过渡态 + 截断对账 |
| 5 | 人不会"挂起等待" | 工具、检索、大模型都有秒级等待 | `PREPARING` / `HOLDING` + T0/T1/T2 填充与兜底 |
| 6 | 有视线、张嘴等视觉线索 | 只有音频（或有限视觉） | 起音确认窗、端点窗不可省，窗口大小即体验参数 |

## 3. 完整场景清单（34 个）

### 阶段 A：静默期与会话边界

| 编号 | 场景 | 状态 / 机制 |
| --- | --- | --- |
| A1 | 会话建立，模型与音频通道预热 | `INITIALIZING`（边缘态） |
| A2 | 双方静默；等待用户 vs 系统主动开场 | `IDLE`，floor = open |
| A3 | 长静默 → 用户 `AWAY` → 主动关怀重启 | away 判定、重启策略 |
| A4 | 系统主动发起（提醒 / 推送到达，用户没在说话） | `INTERJECTING` 的温和版，经打扰管理 |
| A5 | 会话结束（明示 / 挂断 / 超时）与断连恢复 | close / error 事件，重连后状态重建 |

### 阶段 B：用户输入期

| 编号 | 场景 | 状态 / 机制 |
| --- | --- | --- |
| B1 | 疑似起音 → 确认窗 → 确认人声 / 误报（噪声、咳嗽） | 起音确认窗；**外挂 ASR 标识触发点** |
| B2 | 用户说话中，系统伴随反馈 | `BACKCHANNEL`（不拿话轮） |
| B3 | 用户停顿（换气 / 思考，持轮）→ 继续说 or 升级为说完 | 用户 `PAUSED`；后沿窗 + 语义 endpoint |
| B4 | 端点确认，用户释放话轮 | 决策事件 `committed`（话轮转移） |
| B5 | 拒识判定：respond / reject / defer | 决策事件，交互态不变 |
| B6 | 用户超长说话不停顿（无端点） | 强制分段、增量理解 |
| B7 | 旁路人声 / 多人在场（这句是对我说的吗） | 拒识前置判定 + `REPAIR`（"你是在跟我说吗"）；可依赖外挂 ASR |
| B8 | 用户显式禁言（"别说话 / 等一下"） | `MUTED`，解除后回 `LISTENING` |

### 阶段 C：响应准备期（用户说完 → 系统首帧出声）——v2 新增的核心阶段

时延不建状态：`PREPARING` 用三阈值切分——**T0**（约 0.5 s）内静默无感；
超 **T1** 出填充（"嗯，我看一下" / 背景音）；超 **T2** 兜底道歉降级。
工具与大模型只是让 `PREPARING` 变长的原因。

| 编号 | 场景 | 状态 / 机制 |
| --- | --- | --- |
| C1 | 快路径：模型直接答，T0 内出声 | `PREPARING`（用户无感） |
| C2 | 慢生成：超 T1 → 出声占位 | `PREPARING` 的 filler 子阶段 |
| C3 | 说话前先调工具 / 检索（结果决定答案） | `PREPARING` + 事件 tool_started / done / failed |
| C4 | 委托大模型：升级信号 → 等待 → 结果回灌代言 | `PREPARING` + 事件 escalation_started / done |
| C5 | 准备期间用户又开口（追加、改口、催促） | 回到 B1；决策：合并重生成 / 丢弃 / 保留；未播出的生成需服务端丢弃 |
| C6 | 准备超时 / 工具失败 → 兜底话术 | T2 超时事件 |
| C7 | 预提交：B3 停顿期间预生成，端点确认立刻播 | 负时延手段，对应 [latency-discussion](../latency-discussion/) 与 Flux Eager |

### 阶段 D：系统输出期

| 编号 | 场景 | 状态 / 机制 |
| --- | --- | --- |
| D1 | 正常说话，转写按播放进度同步 | `SPEAKING`，锚定播放事实 |
| D2 | 用户附和（"嗯""对"）→ 不让出 | 打断判定窗判为附和 → `SPEAKING` 自环（v1 的 `RESUMING`） |
| D3 | 真打断 → 让出 → 截断对账 | 打断判定窗 → `YIELDING` → `LISTENING` |
| D4 | 假打断 → 暂停待判 → 超时恢复播放 | 暂停待判窗 + resume |
| D5 | 边说边调：说话同时发工具调用，说完时结果已到 | `SPEAKING` + 并行 tool 事件（控制 / 内容双流同帧） |
| D6 | 宣告后持轮等待（"我查一下"说完，工具在跑） | `HOLDING` + filler 策略 |
| D7 | `HOLDING` 期间用户开口：取消 / 改需求 / 催促 / 无关 | 决策：工具取消 or 继续 + 新输入合并 |
| D8 | `HOLDING` 超时 → 道歉 / 降级 | T2 同款兜底 |
| D9 | 说完的去向：释放话轮 vs 提问等答 | floor = open vs floor = agent-expecting-user |
| D10 | 工具缺参数 → 澄清追问 → 等用户答 | D9 特例；超时按默认路径或放弃（对应 phone-assistant `waiting_user`） |

### 贯穿性并发

| 编号 | 场景 | 状态 / 机制 |
| --- | --- | --- |
| X1 | 双方同时开口（碰撞） | floor = contested，让步策略（repair-first vs continuity-first 可配） |
| X2 | 外部事件在任意状态到达（提醒触发） | `INTERJECTING` 放行判定：speak_now / speak_at_pause / hold |
| X3 | 系统需要打断用户（告警类硬插话） | `INTERJECTING` 的硬版，默认禁用 |
| X4 | 语音之外的并行输入（打字、点按） | 本章不展开，仅要求不破坏 floor 语义 |

## 4. 统一状态模型 v2

### 4.1 状态全集

| 类别 | 条目 |
| --- | --- |
| 系统侧·主干 6 态 | `LISTENING` / `PREPARING` / `SPEAKING` / `HOLDING` / `BACKCHANNEL` / `YIELDING` |
| 系统侧·扩展 3 态 | `INTERJECTING`（主动插话，经打扰管理）/ `MUTED`（显式禁言）/ `REPAIR`（澄清"是对我说的吗"） |
| 系统侧·边缘 2 态 | `INITIALIZING` / `IDLE` |
| 用户侧 4 态 | `SPEAKING` / `PAUSED`（持轮停顿）/ `SILENT`（已释放）/ `AWAY` |
| 独立变量 floor | user / agent / open / contested / agent-expecting-user |
| 判定窗口态 | 起音确认窗、后沿端点窗（VAD hangover + 语义）、打断判定窗、暂停待判窗 |
| 决策事件 | 拒识三分、打断三分、`committed`、tool / escalation 生命周期、T1 / T2 超时、floor 转移 |

### 4.2 v1（06 章 10 态）→ v2 对照

| v1 状态 | v2 处置 | 理由 |
| --- | --- | --- |
| LISTENING / SPEAKING / BACKCHANNEL / YIELDING | 保留 | 主干 |
| THINKING / DELEGATING | **拆分**：未出声 → `PREPARING`；出声宣告后的等待 → 并入 `HOLDING` | 两者填充策略（从静默到开口 vs 维持在场感）与超时兜底话术不同；v1 没有覆盖"用户说完到首帧"的时延堆积段 |
| HOLDING | 保留并扩容：自己的轮内停顿 + 宣告后等工具 + `[PAD]` 尾音 | 都是"出过声、仍持轮、暂无新内容" |
| RESUMING | 降级为 `SPEAKING` 自环上的瞬态标注 | 它无时长、无独立 UI 表现，是打断判定窗的一种出口 |
| INTERJECTING / MUTED / REPAIR | 保留为扩展态 | 行为差异足够大，但不进主干图 |
| （无） | 新增判定窗口层与事件层 | v1 把窗口混在守卫表里，未协议化 |

`PREPARING` 与 `HOLDING` 的区分必须立住：前者"话轮已接但未出声"，
后者"说了一半持轮等待"；两者的 T1 填充与 T2 兜底分别配置。

### 4.3 工具调用 / 委托大模型的处理原则

**不建交互态。** 交互层只关心两件事：系统持没持话轮、用户耳朵里是什么。
工具运行中只有两种交互态归属：未出声 = `PREPARING`（C3 / C4），出声宣告过 =
`HOLDING`（D5 / D6）；工具的开始 / 结束 / 失败作为决策事件驱动进出。
委托大模型与工具同构，只多一个"结果由全双工模型代言回灌"的续接动作
（对应 GPT‑Live 委托与 [phone-assistant](../phone-assistant-automation/) 双脑）；
大模型对交互层透明。好处：以后加多少种异步动作（检索、委托、设备控制），
状态图节点数都不变。

## 5. 状态交互图与转换表

图见幻灯片 P7（主干 6 态 + 扩展态 + 用户侧 4 态 + floor；实线 = 系统可观测事实，
虚线 = 判定 / 决策；窗口画成边上的胶囊；每条边标场景编号）。

### 系统侧转换表

| # | 转换 | 触发（●实线 = 系统事实，○虚线 = 判定 / 决策） | 窗口 | 场景 |
| --- | --- | --- | --- | --- |
| 1 | LISTENING → PREPARING | ○ `committed` ∧ respond | 端点窗 | B4 / B5 |
| 2 | PREPARING → SPEAKING | ● `playout_started`（首帧入播放通道） | — | C1 |
| 3 | PREPARING → LISTENING | ○ reject / 用户再开口后弃答 | — | B5 / C5 |
| 4 | PREPARING 自环 | ○ invoke_tool / escalate；● T1 → 填充；● T2 → 兜底 | 时延分段 | C2–C6 |
| 5 | SPEAKING → HOLDING | ● `playout_done` ∧ ○ 持轮（tool pending / 尾音） | — | D5 / D6 |
| 6 | HOLDING → SPEAKING | ● tool_done → 续说（经 PREPARING 语义但不换态） | — | D6 |
| 7 | HOLDING → LISTENING | ○ 取消 / ● T2 兜底后释放 | — | D7 / D8 |
| 8 | SPEAKING → LISTENING | ● `playout_done` ∧ ○ 释放（或 floor = agent-expecting-user） | — | D9 / D10 |
| 9 | SPEAKING → YIELDING | ● `speech_started` → ○ 判为真打断 → interrupt | 打断判定窗 | D3 |
| 10 | YIELDING → LISTENING | ● 停播完成 + 截断对账 | — | D3 |
| 11 | YIELDING → SPEAKING | ● 暂停待判超时 → resume | 暂停待判窗 | D4 |
| 12 | SPEAKING 自环 | ○ 判为附和 / 旁人 / 背景 → 继续（v1 RESUMING） | 打断判定窗 | D2 |
| 13 | LISTENING ⇄ BACKCHANNEL | ○ 附和时机 / 附和结束 | — | B2 |
| 14 | LISTENING → SPEAKING（INTERJECTING） | ○ 高价值信息 ∧ 打扰管理放行 | — | A4 / X2 / X3 |
| 15 | 任意 → MUTED → LISTENING | ○ 用户禁言 / 解除 | — | B8 |
| 16 | 任意 → REPAIR → LISTENING | ○ 没听清 / 指向不明 | — | B7 |

### 用户侧转换表

| 转换 | 触发 | 窗口 | 场景 |
| --- | --- | --- | --- |
| SILENT → SPEAKING | ● `speech_started`（确认） | 起音确认窗 | B1 |
| SPEAKING → PAUSED | ● `speech_stopped` ∧ ○ ¬`committed` | 端点窗 | B3 |
| PAUSED → SPEAKING | ● 再开口 | — | B3 |
| PAUSED / SPEAKING → SILENT | ○ `committed` | 端点窗 | B4 |
| SILENT → AWAY | ● 双方静默超时 | — | A3 |
| AWAY → SILENT | ● 任意活动 | — | A3 |

## 6. 四层分层模型与外部词表校订

### 6.1 四层

| 层 | 内容 | 产地 |
| --- | --- | --- |
| ④ 交互状态层 | 系统 6+3+2 态 / 用户 4 态 / floor | 编排层单写者（LiveKit 模式），只读投影 |
| ③ 事件层（控制面词表） | 感知：`speech_started` / `speech_stopped` / `playout_started` / `playout_done` / tool_done…；决策：`committed` / respond·reject·defer / `invoke_tool` / interrupt·pause·resume / floor 转移；外发：`asr_*` | 感知 = 系统；决策 = 模型或编排层（混合产地） |
| ② 判定窗口层 | 起音确认窗、端点窗、打断判定窗、暂停待判窗、T0/T1/T2 时延分段 | 编排层计时器 + 判定器 |
| ① 信号源层 | 端侧 VAD 能量、模型控制流 token（如 `<shift>` / 分类头）、播放回调（首帧 / 播完）、工具与大模型回调、外部事件 | 各组件 |

### 6.2 外部词表校订

外部给出的定义（7 项）：`speech_started` 感知用户开口 / `speech_stopped` 感知用户说完 /
`committed` 语义完整 / `listen` 本轮说完 / `speak` 决定响应回复 / `invoke_tool` 工具调用和委托 /
`stop` 判定新的一轮、停止播报。

总评：这是一份**单层的事件词表**，与 OpenAI Realtime 控制面几乎一一对应
（`speech_started/stopped` ↔ `input_audio_buffer.*`、`committed` ↔ commit、
`speak` ↔ `response.create`、`stop` ↔ `response.cancel`）。作为**事件层骨架可用**，
但缺窗口层与状态层，且混入了一个状态（`listen`）。逐项：

| 外部定义 | 归类 | 场景 | 校订建议 |
| --- | --- | --- | --- |
| `speech_started` | 感知事件 ✓ | B1 | 保留；拆两段 prearm（疑似）+ confirmed（确认），供外挂 ASR 与打断判定；补 `speech_cancelled`（误报回收） |
| `speech_stopped` | 感知事件 ✓ | B3 | 保留；语义 = 后沿 VAD，**不等于说完**——与 `committed` 的差就是"停顿 vs 说完" |
| `committed` | 决策事件 ✓ | B4 | 保留；= 端点确认 + 话轮释放；建议 payload 带端点置信度（支持 Eager 投机） |
| `listen` | ✗ 状态混入 | D9 | 移出事件层：改为感知事件 `playout_done` + 状态 `LISTENING`；"本轮说完"还需区分释放 vs 提问等答（floor） |
| `speak` | 决策事件 ✓ | B5 / C | **拆三分 respond / reject / defer**——拒识必须显式（日志、外挂 ASR 复核都要用）；payload 带 expect_reply 写 floor |
| `invoke_tool` | 决策事件 ✓ | C3 / C4 / D5 / D6 | 保留，kind = tool / escalate；补生命周期 done / failed；**不改交互态**，只决定 PREPARING / HOLDING 归属 |
| `stop` | 决策 + 动作，语义过载 | D3 / D4 | **拆 interrupt / pause / resume**：真打断 = 停播 + 截断 + 开新轮；假打断 = 暂停待判；"判定新一轮"与"停止播报"解耦——backchannel 不开新轮 |

补充缺失项：`playout_started`（首帧，`SPEAKING` 的锚点——没有它状态就只能锚在模型
开始生成上，字幕与打断对账全错位）、`playout_done`、backchannel（双向）、hold / filler（T1）、
timeout（T2）、away、floor 转移、`asr_*` 外发标识、session close / error。

## 7. 外挂 ASR 的外部启动标识

需求：系统对外给出一个标识，启动外挂 ASR（另有其他用途）。设计：

- **标识挂在判定窗口的进出沿上**（这正是窗口层需要显式建模的证明），两段式：
  - `asr_prearm`：疑似起音（B1 确认窗开启）即发出，外挂 ASR 预热建流，吃掉启动延迟；
  - `asr_start`：起音确认时发出，带 `segment_id` + 起始时间戳（**含确认窗回溯量**，
    音频从起音点回补，不从确认点开始）；
  - `asr_stop`：端点确认（`committed`）时发出，带终止时间戳；
  - `asr_cancel`：确认窗判为误报时回收 prearm。
- **泛化为 segment 级外部协处理触发**：载荷 = `segment_id` + 时间窗 + 音频路由；
  ASR 是第一个订阅者，拒识复核（B5 / B7）、合规记录、热词检测挂同一组信号。
- 产地可以是系统 VAD，将来换成模型控制流输出的起音 / 端点声明，**协议不变**。

## 8. 对模型输出的需求与验收

系统需求层对模型侧的五条需求：

1. **边说边调**：工具调用字段与音频字段同帧共存（D5）；
2. **异步结果中途注入**：任意态下接受注入，模型自行决定接续时机（C4 / D6）；
3. **升级信号 + 代言回灌**：能输出"回答不了、请升级"控制字段；大模型文本回灌代言（C4）；
4. **细分意图控制字段**：`voice_intent`（silence / speak / backchannel / yield）、
   `floor_intent`（keep / release / take）、拒识声明——这是业界二元 listen/speak 之外的增量
   （Instruct‑FD 已证明 Backchannel / Interrupt 是全行业短板）；
5. **共享时间基准**：所有输出携带 `segment_id` 与帧内偏移。

验收两条：

- 给定模型输出流 + 播放侧时间戳，交互层任一状态可**纯函数推导**；
- 五个场景必须跑通：抢话（D3）、假打断（D4）、边说边查（D5）、等待超时（C6 / D8）、
  大模型接管中被打断（C4 × D3）。

## 9. 开放问题

| # | 问题 | 倾向 |
| --- | --- | --- |
| 1 | T0 / T1 / T2 取值 | 0.5 s / 1.5 s / 8 s 起步，按场景类型分档，上线后按打断率与放弃率调 |
| 2 | 控制通道产地 | 混合：物理事实类（在不在出声、播放位置）系统产；意图类（floor、backchannel、升级）模型产 |
| 3 | B7 多人在场判定 | 依赖外挂 ASR 与声纹，第一版暂缓 |
| 4 | X3 反向打断 | 默认禁用，仅告警场景开放 |
| 5 | BACKCHANNEL 独立态 or SPEAKING 变体 | 独立态：不改 floor、不触发字幕与 away 逻辑，行为差异足够大 |
| 6 | 窗口参数（确认窗、端点窗、判定窗时长） | 与 06 章"粒度决定最小时延"同题；作为策略 profile 参数化（repair-first / continuity-first） |
